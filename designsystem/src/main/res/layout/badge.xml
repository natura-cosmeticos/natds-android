<?xml version="1.0" encoding="utf-8"?>
<androidx.constraintlayout.widget.ConstraintLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:id="@+id/badgeContainer"
    android:layout_width="wrap_content"
    android:layout_height="wrap_content"
    android:contentDescription="@string/badge_component_content_description"
    android:focusable="true">

    <ImageView
        android:id="@+id/badgeImage"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:src="@drawable/layer_drawable_base_badge"
        android:visibility="invisible"
        app:layout_constraintBottom_toBottomOf="parent"
        app:layout_constraintEnd_toEndOf="parent"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintTop_toTopOf="parent" />

    <ImageView
        android:id="@+id/badgeRipple"
        android:layout_width="18dp"
        android:layout_height="18dp"
        android:src="@drawable/badge_ripple"
        android:visibility="invisible"
        app:layout_constraintEnd_toEndOf="parent"
        app:layout_constraintTop_toTopOf="parent" />

    <ImageView
        android:id="@+id/badgeDot"
        android:layout_width="?sizeTiny"
        android:layout_height="?sizeTiny"
        android:src="@drawable/badge_dot"
        android:visibility="invisible"
        app:layout_constraintBottom_toBottomOf="@id/badgeRipple"
        app:layout_constraintEnd_toEndOf="@id/badgeRipple"
        app:layout_constraintStart_toStartOf="@id/badgeRipple"
        app:layout_constraintTop_toTopOf="@id/badgeRipple"
        app:tint="@color/colorBrdNatRed" />

</androidx.constraintlayout.widget.ConstraintLayout>